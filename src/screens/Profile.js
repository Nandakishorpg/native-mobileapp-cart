import { React, useState } from "react";
import {
  View,
  Text,
  ScrollView,
} from "react-native";

const Profile = () => {
  return (
    <View>
      <ScrollView>
        <View >
          <Text className="text-red-500" >Profile page </Text>
        </View>
      </ScrollView>
    </View>
  );
};



export default Profile;
